spring.application.name=Demohs

#
## Database
spring.datasource.url=jdbc:postgresql://localhost:5432/Demohs
spring.datasource.username=postgres
spring.datasource.password=4405
#
#eureka.instance.hostname = localhost


# Hibernate properties
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# Logging SQL statements (optional)
spring.jpa.show-sql=true

server.port=9090
#eureka.instance.client.serverUrl.defaultZone=http://localhost:8761
#eureka.instance.prefer-ip-address=true

#spring.config.import=optional:configserver:http://localhost:8888

spring.web.cors.allowed-origins=http://localhost:3000
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE
spring.web.cors.allowed-headers=*